<div class="item-border {{type}}">
  <div class="skill-card skill" data-item-id="{{_id}}">
    <div class="skill-name"><h3>
        {{#if inSheet}}
          <a title="{{localize "l5r4.sheet.edit"}}" class="item-edit">{{name}}</a>
        {{else}}
          {{name}}
        {{/if}}
      </h3>
    </div>
    <div class="skill-info skill-stats">
      <div class="skill-type">
          {{localize "l5r4.skill.type"}}: {{localize (concat "l5r4.skillTypes." system.type)}}
      </div>
      <div class="skill-rank">
        {{localize "l5r4.skill.rank"}}: {{system.rank}} {{#if system.school}}{{localize "l5r4.skill.school"}}{{/if}}
      </div>
      {{#if system.emphasis}}
        <div class="skill-emphasis">
          {{localize "l5r4.skill.emphasis"}}: {{system.emphasis}}
        </div>
      {{/if}}
      {{#unless inSheet}}
      <div class="spell-effects">
        {{{system.specialRules}}}
      </div>
      {{/unless}}
      {{#if (gte system.rank 3) }}
        {{#if system.mastery_3}}
          <div>
            <p>{{localize "l5r4.skill.mastery3"}}: {{system.mastery_3}}</p>
          </div>
        {{/if}}
      {{/if}}
      {{#if (gte system.rank 5) }}
        {{#if system.mastery_5}}
          <div>
            <p>{{localize "l5r4.skill.mastery5"}}: {{system.mastery_5}}</p>
          </div>
        {{/if}}
      {{/if}}
      {{#if (gte system.rank 7) }}
        {{#if system.mastery_7}}
          <div>
            <p>{{localize "l5r4.skill.mastery7"}}: {{system.mastery_7}}</p>
          </div>
        {{/if}}
      {{/if}}
      {{#if (gte system.insight bonus) }}
        {{#if system.insight_bonus}}
          <div>
            <p>{{localize "l5r4.skill.insightbonus"}}: {{system.insight_bonus}}</p>
          </div>
        {{/if}}
      {{/if}}
    </div>
    {{#if inSheet}}
      <div>
        <a class="item-edit"  title="{{localize "l5r4.sheet.edit"}}"><i class="fas fa-edit fa-fw"></i></a>
      </div>
    {{/if}}
  </div>
</div>